/**
 * ActionHistory.js - Placeholder for the action history component
 */
export class ActionHistory {
    constructor(container, devTools) {
        this.container = container;
        this.devTools = devTools;
        this.container.innerHTML = '<div>Action History (placeholder)</div>';
    }
    updateActionHistory() {}
    clearHistory() {}
    destroy() {}
} 