// Add modal imports at the top with other imports
import { initializePublishModalIntegration } from './components/PublishModalIntegration.js';

// Initialize the modal integration (this replaces the ugly confirm/alert dialogs)
initializePublishModalIntegration(); 