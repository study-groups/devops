# Base Agent Profile - Works for both Sonnet 4.1 and GPT-5
# Universal instructions that should work well across different LLMs

AGENT_NAME="base"
AGENT_DESCRIPTION="Universal base profile for most LLMs"

# Instruction template that gets added to MULTICAT output
AGENT_INSTRUCTION_TEMPLATE="# Instructions for AI Assistant

Please generate code modifications in raw MULTICAT format.

## Critical Rules:
- Output ONLY raw MULTICAT format (no markdown code blocks)
- Start immediately with #MULTICAT_START
- Use relative paths like ./src/filename.ext
- Include brief descriptions in # note: field
- Do not explain or introduce your changes

## Template:
#MULTICAT_START
# dir: ./src
# file: filename.ext
# note: brief description
#MULTICAT_END
[raw file content here]

"

# Example template for --example base
AGENT_EXAMPLE_TEMPLATE="#MULTICAT_START
# dir: ./src
# file: utils.js
# note: Universal utility functions
#MULTICAT_END
// Simple utility functions that work across projects
export const isEmpty = (value) => {
    return value == null || value === '' || (Array.isArray(value) && value.length === 0);
};

export const debounce = (func, wait) => {
    let timeout;
    return function executedFunction(...args) {
        const later = () => {
            clearTimeout(timeout);
            func(...args);
        };
        clearTimeout(timeout);
        timeout = setTimeout(later, wait);
    };
};

#MULTICAT_START
# dir: ./config
# file: constants.js
# note: Application constants
#MULTICAT_END
export const API_BASE_URL = process.env.API_URL || '/api';
export const DEFAULT_TIMEOUT = 5000;
export const RETRY_ATTEMPTS = 3;
"

# Output preferences
PREFER_CONCISE_OUTPUT="true"
INCLUDE_CONTEXT_HINTS="true"
MAX_SUGGESTED_FILES="20"